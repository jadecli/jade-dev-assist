name: CI

on:
  pull_request:
    branches: [main, jadecli-main]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
      - run: npm ci --ignore-scripts
      - name: Validate plugin structure
        run: node scripts/validate-plugin.js . || true
      - name: Test
        run: npm test
        continue-on-error: true  # test-plugin requires .claude-plugin/ (not in repo)
